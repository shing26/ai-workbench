# AI Workbench Handoff — v1.1-alpha

日期：2026-08-10
仓库：D:\ai-workbench（分支 `develop`，Tag `v1.1-alpha` 已推送）
上家：`docs/handoffs/2026-08-07-v1-complete-handoff.md`（v1.0 状态）

## 当前状态

- **v1.1-alpha 已封存**：Sprint 7~10 全部完成，12 条 AC 达成（AC-4.2 核心达成），客户端打包（exe+MSI+NSIS），Tag 已推送。
- 最新提交：`e5a6f09`（feat(s10): i18n hot-swap + API hot-swap + snapshot cleanup）。
- 发行归档：`docs/RELEASE-v1.1-alpha.md`（Sprint 7~10 战果 + 质量门 + AC 矩阵）。
- PRD：`docs/plans/hermes-station-prd.md`（Single Source of Truth）。

## Sprint 7~10 战果速览

| Sprint | 主题 | 关键交付 |
|---|---|---|
| 7 | 资产安全 | `.hermes/backups` 阴影快照 + `[⚡ Apply]` 原子写入 + ⌘Z 撤销 + FILE_UPDATED 广播 |
| 8 | 工程闭环 | tasks.project_id/is_dod + DoD 双向削减 + Inline Diff 树 + Hunk 抽屉 + Quality Gate 顶栏 |
| 9 | 极客体验 | Linear 键盘 j/k/x/p/a + AI 3 步拆解 + MOA 3s 熔断 |
| 10 | 国际与稳健 | i18n 热切（zh/en/ja）+ API 热切 + 快照清理 |

## 质量门基线

cargo **220/220** / clippy / fmt / tsc / lint / prettier / build / **verify:preview** 全绿。

## 工作区注意事项（延续）

- 用户文件不改不提交：`package.json`、`src/lib/searchHistory.ts`、`src/lib/tokenBudget.ts`。
- 浏览器 fallback 与 Rust 双端同构：`src/lib/db.ts` ↔ `src-tauri/src/db.rs`。
- dev server `localhost:1420`（浏览器模式）；桌面 `npm run tauri dev`；打包 `npm run tauri build`。
- 命令注册在 `src-tauri/src/lib.rs` 的 `invoke_handler`；新命令须同步注册。

## 关键实现位置（后续迭代参考）

- 快照/Apply/Rollback：`src-tauri/src/file_ops.rs`（`apply_code_snippet`/`rollback_snapshot`/`prune_snapshots`）
- Git diff 树/Hunk：`src-tauri/src/lib.rs`（`get_project_diff_tree`/`get_file_hunk_patch`）+ `src/views/ProjectsView.tsx`
- Quality Gate：`src-tauri/src/lib.rs`（`run_quality_gate`）+ `AppHeader`
- MOA 熔断：`src/lib/db.ts`（`firstTokenTimeoutMs` 3000ms）
- i18n：`src/lib/i18n.ts`（字典 + `useLocale` + localStorage 持久化）
- 事件总线：`src/hooks/useTauriEvents.ts`（FILE_UPDATED/FILE_RESTORED/DOD_STATUS_CHANGED）
- 上下文管线：`workbenchStore.vibeContext/noteContext/actionContext`

## 遗留与后续方向

- **Keyring 全量审计迁移**（AC-4.2 延后项）→ `v1.1.1-patch` 独立维护：`.scratch/hermes-station/issues/11-s10-keyring-hardening.md`。含明文 key 一键搬入 Keychain、启动自检、localStorage 明文断言。
- MOA 熔断桌面端回归（浏览器 fallback 无真实流验证）。
- i18n 字典扩展至其余视图。
- v1.0 遗留（Connection Layer / Monetization）仍搁置。
- 未来：v1.1.1-patch（Keyring）→ v1.2（可选项，需用户确认新范围）。

## suggested skills

- 继续开发 Keyring 加固 / 新模块：`implement`
- 排查桌面端 MOA 或运行时问题：`diagnosing-bugs`
- 代码审查或合并前自检：`code-review`
- 拆新方向为 ticket：`to-spec` / `to-tickets`
